# Unit Tests

Run `npm run test:unit`. Unit tests cover deterministic domain behavior such as the call state machine, configuration/readiness, concurrency leases, calling windows, encryption helpers, qualification, orchestration, and improvement lifecycle gates.
