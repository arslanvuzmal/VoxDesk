# Security Tests

Run `npm run test:security`. The suite exercises tenant isolation, campaign authorization, call/contact/opportunity access, signed conversation contexts, web voice/text boundaries, webhook verification, sensitive identifier handling, recording policy, and legacy mutation-route retirement.
