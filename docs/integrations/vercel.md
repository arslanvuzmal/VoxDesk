# Vercel

Vercel hosts the portfolio deployment and Preview environments. Preview validates a commit artifact; it does not implicitly apply migrations or certify a provider integration. Promotion requires exact-SHA verification and the production smoke checks in [deployment operations](../operations/deployment.md).
