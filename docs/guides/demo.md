# Running the Demo

The public demo is a controlled portfolio workflow. It can use web voice when configured and uses deterministic telephony simulation for phone scenarios.

Simulation creates records through the real application state machine and authorized tool path. Every resulting call/conversation is marked `SIMULATION`; it is not evidence of PSTN routing or paid carrier execution.
